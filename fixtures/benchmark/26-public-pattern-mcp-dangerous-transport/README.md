Reduced MCP registry transport fixture inspired by public registry remotes and
package transport metadata.

The fixture exercises local bridge remotes, unauthenticated remotes,
secret-bearing headers, stdio packages, and shell-wrapper commands without
installing or running an MCP server.

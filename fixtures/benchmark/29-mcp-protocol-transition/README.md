# MCP protocol transition fixture

This deterministic fixture records a legacy server, a server advertising both
legacy and modern revisions, and a server without an explicit revision. It is a
static migration review input and never starts or negotiates an MCP server.

# MCP Tasks capability fixture

This synthetic fixture declares the official MCP Tasks extension and its exact
poll, update, and cancellation method names. The scanner records those
surfaces as advisory capabilities; it does not start a server or infer that
`run_job` creates durable work.

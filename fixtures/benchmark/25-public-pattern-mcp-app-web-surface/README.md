Reduced MCP Apps/WebMCP-style metadata fixture inspired by public app-surface
metadata patterns.

The fixture keeps only origin-like fields, UI/resource hints, and a dynamic
tool registration concept so static scanning can review the declared surface.

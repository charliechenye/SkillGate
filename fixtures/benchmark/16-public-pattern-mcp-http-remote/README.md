# Public Pattern MCP HTTP Remote

Reduced, nonverbatim example based on the public `modelcontextprotocol/servers`
remote HTTP `.mcp.json` shape and MCP transport documentation.

Expected: SG003 and SG009.

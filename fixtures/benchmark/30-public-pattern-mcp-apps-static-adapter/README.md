Reduced MCP Apps fixture for the static adapter.

The fixture declares a modern `_meta.ui.resourceUri`, MCP Apps HTML MIME type,
CSP origins, a browser permission, and an app-callable tool. The bundled HTML,
CSS, and JavaScript are local fixture files so SkillGate can inventory app
assets and host bridge markers without rendering or executing the UI.

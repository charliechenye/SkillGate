Reduced MCP registry package metadata fixture inspired by official registry
server records that include repositories, remotes, and package identifiers.

This case supports static scan coverage and registry comparison tests for
repository, package, transport, and remote URL drift.

Reduced MCP tool metadata fixture inspired by public MCP registry server
metadata and tool declaration patterns.

The examples are intentionally synthetic and minimal. They exercise hidden
instruction language, suspicious tool names, and risky input schema fields
without copying upstream server content.

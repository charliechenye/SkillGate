# Reviewable demo skill

This synthetic Agent Skill is intentionally reviewable. Its helper script
fetches a remote template and passes it to a shell so a first-run SkillGate
session can show capability findings without executing the helper.

The domain uses `.invalid`; no network request is expected to succeed.
